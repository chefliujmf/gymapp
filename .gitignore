node_modules
dist
dist-ssr
dev-dist
*.tsbuildinfo
*.local
.DS_Store
.env
.env.*
!.env.example

# Scraped / personal / imported content — never commit
src/data/centr/*.json
src/data/coach/*.json
src/data/recipes/*.json
public/media/
.secrets/

# Scraped media staging (~10GB: 5.4GB meditation audio + 3.9GB recipe images).
# Never commit — audio lives on the XPS server, backed up to Google Drive.
downloaded_pages/

# generated catalog (derived from personal scraped content)
src/data/generated/
# ...but swim-workouts.json is AUTHORED content (scripts/build-swim-workouts.mjs), not scraped — commit it so
# build:app (prod, which doesn't run build-catalog) and CI both have it. #swim-tri
!src/data/generated/swim-workouts.json
server/node_modules
server/data
*.env
auth.env
server/dev-data
media-opt

# cyclingcoach engine copied in (#fork) — books are 173MB copyrighted binaries,
# local-only (cyclingcoach gitignores them too). The method + notes ARE committed.
coach-engine-src/knowledge_base/
coach-engine-src/.venv/
coach-engine-src/.secrets/
coach-engine-src/**/__pycache__/
coach-engine-src/**/*.apk
pgdata/

# Personal activity files (never commit)
*.fit
*.gpx
*.tcx

# raw source books (copyrighted, distilled into docs/*-coaching.md — never commit)
books/
*.epub
*.azw3
*.mobi
