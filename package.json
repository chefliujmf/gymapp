{
  "name": "gymapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Personal self-hosted fitness app (Centr-style) — installable PWA",
  "scripts": {
    "dev": "vite",
    "dev:api": "bash scripts/dev-api.sh",
    "dev:full": "concurrently -k -n api,web -c green,cyan \"npm:dev:api\" \"npm:dev\"",
    "build": "node scripts/build-catalog.mjs && tsc -b && vite build",
    "preview": "vite preview",
    "seed:centr": "node scripts/seed-from-centr.mjs",
    "build:catalog": "node scripts/build-catalog.mjs",
    "deploy": "bash scripts/deploy.sh",
    "build:app": "tsc -b && vite build",
    "sync:catalog": "bash scripts/sync-catalog.sh",
    "deploy:staging": "bash scripts/deploy-staging.sh"
  },
  "dependencies": {
    "@simplewebauthn/browser": "^13.3.0",
    "dexie": "^4.0.10",
    "dexie-react-hooks": "^1.1.7",
    "lucide-react": "^1.20.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.1"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@types/web-bluetooth": "^0.0.21",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^10.0.3",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vite-plugin-pwa": "^0.21.1"
  }
}
