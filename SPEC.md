# GymApp — Product Spec (single source of truth)

> Edit this file to give full instructions. I build to it in coherent chunks and
> we review against it — not screen-by-screen. Anything marked **[NEEDS YOU]** is
> an open decision. Anything marked **[CLONE: x]** means faithfully match that app.

## 1. Vision
A personal, **local-first web PWA** for one user, consolidating **your own paid
subscriptions** into one app. Hostable on **Vercel**. Personal use only.
**Emby out of scope. The word "Centr" must not appear anywhere in the app**
(IDs, titles, copy — only media-asset URLs may retain the CDN host).

### Core architecture — planning vs execution
- **intervals.icu = the PLAN / source of truth.** The coach (the `cyclingcoach`
  repo) publishes the calendar there: structured indoor rides, gym sessions, and
  embedded meal/snack recommendations. It also drives outdoor rides via Wahoo.
- **gymapp = the EXECUTION layer for INDOOR sessions.** It reads the plan from
  intervals.icu and *executes* it (ride the indoor workout, do the gym session).
  Outdoor riding is handled by intervals.icu ↔ Wahoo — gymapp does not touch it.
- So gymapp's **Today** = "what intervals.icu planned for today" → tap to execute;
  surface the day's meal/snack recs → link to recipes. JOIN library = the browse-all
  catalog; the *planned* ride's structure comes from the intervals.icu event itself.

## 2. Reference apps (what to clone for each area)
- **Gym "do a workout"** → **[CLONE: Centr]** timed follow-along player. (Confirmed:
  all 139 gym workouts are *timed circuits* — "40s" exercises, zero weight×reps — so
  LiftLog's set-logging does NOT fit; Centr's player does.)
- **Cycling / Running** → **[CLONE: JOIN (join.cc)]** for browse/detail look, and
  **[CLONE: wolfv/cycling-workout]** for the in-browser interval *player* (moving
  profile, target power, Web Bluetooth trainer, .zwo/.tcx).
- **Food / meal plan** → **[CLONE: Mealie]** (recipe cards, ingredient/step layout,
  weekly meal planner, shopping list, servings scaler).
- **Meditation** → **[CLONE: Centr]** Mind.
- **App shell / history / local-first patterns** → **[CLONE: LiftLog]**.

## 3. Data (collected, gitignored, regenerated by `npm run build:catalog`)
| Domain | Count | Source | Notes |
|---|---|---|---|
| Recipes + snacks | 660 | Centr | macros, ingredients (decoded units), steps, image |
| Gym workouts | 139 | Centr | timed circuits; each exercise has image + demo video |
| Meditations | 109 | Centr | audio mp3 (10GB on XPS + Drive `gymapp/audio`) |
| Cycling | 494 | JOIN | structured intervals (duration + %FTP + W + HR) |
| Running | 302 | JOIN | same structure |
| Cycling **plan** | — | **intervals.icu** | the COACH's schedule — bridge needed |

## 4. Information architecture (bottom nav)
`Today` · `Train` (gym) · `Ride` (cycling+running) · `Eat` · `Mind` · `Profile`

## 5. Domains & screens

### Train (gym) — Centr model
- Library: search + discipline filter + cards. **[DONE]**
- Detail: exercise list, each with image + tap-to-play video. **[DONE]**
- **Player [CLONE: Centr] [TODO]**: Start → fullscreen guided player:
  get-ready countdown → for each exercise: video + big countdown (`40s`) + `n/total`
  → rest countdown → next → **Complete** screen → log to history. Controls:
  **pause/resume, skip, back, STOP/exit**. Wake-lock. Resume-after-background.
- History/Progress: sessions, streak, calendar.

### Ride (cycling + running) — JOIN + wolfv
- Library: sport toggle + category + search + profile-graph cards. **[DONE]**
- Detail: profile graph, structure, TSS, `.zwo` export. **[DONE]**
- **Player [CLONE: wolfv] [TODO]**: Start → live moving profile, elapsed/remaining,
  current target (W from FTP, %FTP), interval countdown, lap list. Web-Bluetooth ERG
  to smart trainer (later). `.tcx` export after.
### intervals.icu bridge [TODO — the keystone feature]
- **API**: `GET https://intervals.icu/api/v1/athlete/{id}/events?oldest=YYYY-MM-DD&newest=…`,
  Basic auth `API_KEY:<key>`. Athlete `i28814`; key in `cyclingcoach/.secrets/coach.env`
  (`INTERVALS_ICU_API_KEY`). **FTP = 260 W** (`cyclingcoach/.../ftp_estimate.md`) → %FTP→W.
- **Key handling**: user pastes key + athlete id in **Settings** (IndexedDB). Browser↔
  intervals.icu has CORS limits → **proxy** (vite dev proxy locally; Vercel function in prod).
- **Event model** (per day): `category` = `WORKOUT` (executable) | `TARGET` (weekly macro
  goal → context only). `type` = `Ride` (has `workout_doc`) | `WeightTraining` (gym).
  Fields: `name`, `start_date_local`, `moving_time` s, `icu_training_load` (TSS), `description`.
- **`workout_doc.steps`**: each step is `{power:{start,end,units:"%ftp"}, duration}` (steady/ramp)
  or a repeat `{reps, steps:[…]}`. Normalize to the SAME interval model as JOIN → one player
  plays both. Ramp = start%→end% FTP over duration.
- **Nutrition**: description has `## Fueling / Meals / Supplements` → `Centr recipe: <name>, <url>`
  lines → parse, match `<name>` to a library recipe, link in (strip brand name).
- **Today flow**: today's `WORKOUT` events → Ride→indoor player (workout_doc scaled to FTP),
  WeightTraining→gym execution; show the day's meal/snack recs.

### Eat — Mealie model
- Recipe library: search + filter + cards. **[DONE]**
- Recipe detail [CLONE: Mealie] [TODO]: hero image, ingredients (with **servings
  scaler**), numbered steps, macros, source link.
- Meal planner [CLONE: Mealie] [TODO]: weekly grid, assign/swap recipes per slot.
- Shopping list [CLONE: Mealie] [PARTIAL]: aggregated, checkable, grouped.

### Mind — Centr
- Library: search + kind filter. **[DONE]**
- Detail [TODO]: audio player. **[NEEDS YOU]** serve audio from XPS/Drive, or stream?

### Profile / Settings [TODO]
- **FTP** (for cycling watts), units (kg/lb), goals, data export.

## 6. Tech & constraints
- Vite + React + TS, Dexie (IndexedDB), vite-plugin-pwa. Local-first; no backend.
- Catalog generated → `src/data/generated/` (gitignored). `npm run build:catalog`.
- Host: **Vercel** (free `*.vercel.app`). **[NEEDS YOU]** public internet vs keep private?
- No Emby. No content rehosting. Mobile-first.

## 7. Build order
1. ✅ Data import + catalog wiring
2. ✅ Browse / search / select all domains
3. ⏳ **Gym Centr player** · **Ride wolfv player** · **intervals.icu bridge**
4. Eat (Mealie) · Mind audio · Profile/FTP
5. PWA polish + Vercel deploy

## 8. Decisions (resolved 2026-06-17)
1. **intervals.icu**: ✅ key in `cyclingcoach/.secrets/coach.env`, athlete `i28814`.
   Bridge is **READ-ONLY** (gymapp executes; the coach owns publishing). The plan IS
   the source of truth — Today comes from intervals.icu, not the JOIN library.
2. **Meditation audio**: serve from the **Google Drive URL** (the `gymapp/audio` copy).
   **[NEEDS YOU]** the shareable Drive base URL / how files are addressed.
3. **Self-host media**: defer to the **end**, once the app is final.
4. **FTP = 260 W** + athlete numbers live in `cyclingcoach` (`ftp_estimate.md`).
5. **Hosting**: Vercel, decided at the end.
6. Still helpful: any screen to pixel-clone → screenshots in `docs/refs/`.
